#include <stdio.h>
int main() {
    int a[] = {10, 25, 30, 41, 56}, i;
    int n = sizeof(a) / sizeof(a[0]);

    for (i = 0; i < n; i++) {
        if (a[i] % 2 == 0)
            printf("%d is Even\n", a[i]);
        else
            printf("%d is Odd\n", a[i]);
    }

    return 0;
}
